/*
 * Функция unexpected() вызывается если …
 * 1) исключение не соответствует спецификации исключений (x)
 * 2) сгенерированное исключение покидает блок try уровня функции
 * 3) в программе возникло необработанное исключение
 * 4) исключение генерируется в деструкторе
 * 5) исключение генерируется в конструкторе
 */
#include<iostream>
using namespace std;
int main () {
	return 0;
}
